apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: appicontester-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    namespace: appicontester-development
    server: https://kubernetes.default.svc
  source:
    repoURL: git@github.com:jojobobby/AppIconWebsite.git
    targetRevision: development
    path: k8s/development
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
      - ServerSideApply=false
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m
  revisionHistoryLimit: 5
